'use strict';
(() => {
  const PLAN_KEY='just-celebrate-private-planner-v1';
  const STATUS_KEY='just-celebrate-service-status-v1';
  const categories={venue:{name:'Venue',find:'Find venues',hash:'vendors-venues'},music:{name:'DJ & music',find:'Find DJs',hash:'vendors-djs-music'},photography:{name:'Photography',find:'Find photographers',hash:'vendors-photography-video'},catering:{name:'Catering',find:'Find caterers',hash:'vendors-catering'},cake:{name:'Cake & treats',find:'Find cake suppliers',hash:'vendors-cakes-treats'},decor:{name:'Decor & balloons',find:'Find decorators',hash:'vendors-decor-balloons'},entertainment:{name:'Entertainment',find:'Find entertainment',hash:'vendors-entertainment'},transport:{name:'Transport',find:'Find transport',hash:'vendors-transport'}};
  const statusOrder=['Not started','Vendors saved','Enquiry sent','Replied','Booked'];
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const readPlan=()=>{try{return JSON.parse(localStorage.getItem(PLAN_KEY)||'null')}catch{return null}};
  const readStatuses=()=>{try{return JSON.parse(localStorage.getItem(STATUS_KEY)||'{}')}catch{return {}}};
  const writeStatuses=v=>{try{localStorage.setItem(STATUS_KEY,JSON.stringify(v))}catch{}};
  function inferredStatus(id,plan){const suppliers=(plan.suppliers||[]).filter(s=>s.category===id);if(!suppliers.length)return 'Not started';if(suppliers.some(s=>s.status==='Booked'))return 'Booked';if(suppliers.some(s=>s.status==='Contacted'))return 'Enquiry sent';return 'Vendors saved';}
  function dateLabel(value){if(!value)return 'Date to be confirmed';try{return new Date(value+'T12:00:00').toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'long',year:'numeric'})}catch{return value}}
  function visualPlan(plan,saved){const services=(plan.services||[]).filter(id=>categories[id]);const serviceCards=services.map(id=>{const c=categories[id],suppliers=(plan.suppliers||[]).filter(s=>s.category===id),status=statusOrder.includes(saved[id])?saved[id]:inferredStatus(id,plan);return `<article class="visual-service"><div class="visual-service-top"><span class="visual-service-name">${esc(c.name)}</span><span class="visual-status ${status==='Booked'?'booked':''}">${esc(status)}${status==='Booked'?' ✓':''}</span></div>${suppliers.length?`<div class="visual-suppliers">${suppliers.map(s=>`<div class="visual-supplier"><strong>${esc(s.name)}</strong><span>${esc(s.status||'Shortlisted')}</span>${s.website?`<a href="${esc(s.website)}" target="_blank" rel="noreferrer">View supplier ↗</a>`:''}</div>`).join('')}</div>`:`<div class="visual-empty">Still to arrange <a href="/planner#${c.hash}">Find ${esc(c.name.toLowerCase())} →</a></div>`}</article>`}).join('');const name=plan.details?.name||plan.details?.occasion||'My celebration';return `<div class="visual-plan-shell"><div class="visual-plan-hero"><p>MY CELEBRATION</p><h2>${esc(name)}</h2><div class="visual-plan-meta"><span>${esc(plan.details?.occasion||'Celebration')}</span><span>${esc(dateLabel(plan.details?.date))}</span><span>${esc(plan.details?.location||'Location to be confirmed')}</span><span>${plan.details?.guests?esc(plan.details.guests)+' guests':'Guest list in progress'}</span></div></div><div class="visual-plan-message"><span>✦</span><div><strong>It’s coming together.</strong><p>Your chosen suppliers and the pieces still to arrange, all in one lovely plan.</p></div></div><div class="visual-services">${serviceCards}</div><div class="visual-plan-footer"><button type="button" class="visual-back">← Back to Planning Portal</button><button type="button" class="visual-print">Print / save plan</button></div></div>`}
  function openVisualPlan(){const plan=readPlan();if(!plan)return;const workspace=document.querySelector('#workspace');if(!workspace)return;workspace.dataset.portalBackup=workspace.innerHTML;workspace.innerHTML=visualPlan(plan,readStatuses());workspace.scrollIntoView({behavior:'smooth',block:'start'});workspace.querySelector('.visual-back')?.addEventListener('click',()=>{workspace.innerHTML=workspace.dataset.portalBackup||'';delete workspace.dataset.portalBackup;requestAnimationFrame(upgradePortal)});workspace.querySelector('.visual-print')?.addEventListener('click',()=>window.print());}
  function upgradePortal(){const workspace=document.querySelector('#workspace'),portal=document.querySelector('#portal-content');if(!workspace||!portal||document.querySelector('#service-plan-overview'))return;const plan=readPlan();if(!plan||plan.step!==3||!Array.isArray(plan.services)||!plan.services.length)return;const saved=readStatuses();const cards=plan.services.filter(id=>categories[id]).map(id=>{const c=categories[id],inferred=inferredStatus(id,plan),status=statusOrder.includes(saved[id])?saved[id]:inferred,supplierCount=(plan.suppliers||[]).filter(s=>s.category===id).length;return `<article class="service-plan-card ${status==='Booked'?'is-booked':''}"><div class="service-plan-copy"><div class="service-plan-heading"><h3>${esc(c.name)}</h3><span class="service-plan-status">${esc(status)}${status==='Booked'?' ✓':''}</span></div><p>${supplierCount?supplierCount+' vendor'+(supplierCount===1?'':'s')+' saved':'Ready when you are'}</p></div><div class="service-plan-actions"><select data-plan-status="${esc(id)}" aria-label="Planning status for ${esc(c.name)}">${statusOrder.map(s=>`<option ${s===status?'selected':''}>${esc(s)}</option>`).join('')}</select><a class="service-find-button" href="/planner#${c.hash}">${esc(c.find)} <span aria-hidden="true">→</span></a></div></article>`;}).join('');const finished=plan.services.filter(id=>(saved[id]||inferredStatus(id,plan))==='Booked').length;const overview=document.createElement('section');overview.id='service-plan-overview';overview.className='service-plan-overview';overview.innerHTML=`<div class="service-plan-intro"><div class="service-plan-intro-copy"><p class="service-plan-kicker">YOUR CELEBRATION PLAN</p><h3>${finished} of ${plan.services.length} services sorted</h3><p>Work through each part at your own pace. Your next step is always right here.</p></div><button type="button" class="view-visual-plan">View my celebration plan <span>→</span></button><div class="service-plan-progress" aria-label="${finished} of ${plan.services.length} services booked"><span style="width:${plan.services.length?finished/plan.services.length*100:0}%"></span></div></div><div class="service-plan-list">${cards}</div>`;portal.prepend(overview);overview.querySelector('.view-visual-plan')?.addEventListener('click',openVisualPlan);overview.querySelectorAll('[data-plan-status]').forEach(select=>select.addEventListener('change',()=>{const next=readStatuses();next[select.dataset.planStatus]=select.value;writeStatuses(next);overview.remove();upgradePortal();}));}
  function addStartNew(){const nav=document.querySelector('.planner-header-actions');if(!nav||document.querySelector('.start-new-celebration'))return;const link=document.createElement('a');link.className='start-new-celebration';link.href='/celebration-planner/index.html?fresh=1';link.textContent='+ Start a new celebration';link.addEventListener('click',e=>{if(!confirm('Start a new celebration? Your current plan will be kept as a backup in this browser.'))e.preventDefault();});nav.insertBefore(link,nav.firstChild);}
  function run(){addStartNew();upgradePortal();}const observer=new MutationObserver(()=>requestAnimationFrame(run));window.addEventListener('DOMContentLoaded',()=>{run();const workspace=document.querySelector('#workspace');if(workspace)observer.observe(workspace,{childList:true,subtree:true});});window.addEventListener('storage',run);
})();
