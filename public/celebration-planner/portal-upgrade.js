'use strict';
(() => {
const PLAN_KEY='just-celebrate-private-planner-v1',STATUS_KEY='just-celebrate-service-status-v1';
const categories={venue:{name:'Venue',find:'Find venues',hash:'vendors-venues'},music:{name:'DJ & music',find:'Find DJs',hash:'vendors-djs-music'},photography:{name:'Photography',find:'Find photographers',hash:'vendors-photography-video'},catering:{name:'Catering',find:'Find caterers',hash:'vendors-catering'},cake:{name:'Cake & treats',find:'Find cake suppliers',hash:'vendors-cakes-treats'},decor:{name:'Decor & balloons',find:'Find decorators',hash:'vendors-decor-balloons'},entertainment:{name:'Entertainment',find:'Find entertainment',hash:'vendors-entertainment'},transport:{name:'Transport',find:'Find transport',hash:'vendors-transport'}};
const statusOrder=['Not started','Vendors saved','Enquiry sent','Replied','Booked'];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const readPlan=()=>{try{return JSON.parse(localStorage.getItem(PLAN_KEY)||'null')}catch{return null}},readStatuses=()=>{try{return JSON.parse(localStorage.getItem(STATUS_KEY)||'{}')}catch{return {}}},writeStatuses=v=>{try{localStorage.setItem(STATUS_KEY,JSON.stringify(v))}catch{}};
function inferredStatus(id,p){const s=(p.suppliers||[]).filter(x=>x.category===id);if(!s.length)return'Not started';if(s.some(x=>x.status==='Booked'))return'Booked';if(s.some(x=>x.status==='Contacted'))return'Enquiry sent';return'Vendors saved'}
function dateLabel(v){if(!v)return'Date to be confirmed';try{return new Date(v+'T12:00:00').toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'long',year:'numeric'})}catch{return v}}
function visualPlan(p,saved){const services=(p.services||[]).filter(id=>categories[id]);const cards=services.map(id=>{const c=categories[id],ss=(p.suppliers||[]).filter(x=>x.category===id),status=statusOrder.includes(saved[id])?saved[id]:inferredStatus(id,p);return `<article class="visual-service"><div class="visual-service-top"><span class="visual-service-name">${esc(c.name)}</span><span class="visual-status ${status==='Booked'?'booked':''}">${esc(status)}${status==='Booked'?' ✓':''}</span></div>${ss.length?`<div class="visual-suppliers">${ss.map(x=>`<div class="visual-supplier"><div><strong>${esc(x.name)}</strong><span>${esc(x.status||'Shortlisted')}</span></div><div class="visual-supplier-actions">${x.website?`<a href="${esc(x.website)}" target="_blank" rel="noreferrer">View profile</a>`:''}<a href="/planner#${c.hash}">Send enquiry</a><button type="button" data-book-service="${esc(id)}">Booked ✓</button></div></div>`).join('')}</div>`:`<div class="visual-empty">Still to arrange <a href="/planner#${c.hash}">Find ${esc(c.name.toLowerCase())} →</a></div>`}</article>`}).join('');const name=p.details?.name||p.details?.occasion||'My celebration';return `<div class="visual-plan-shell"><div class="visual-plan-toolbar"><button type="button" class="visual-back">← Back to My Planning Portal</button><span>Your visual celebration plan</span></div><div class="visual-plan-hero"><p>MY CELEBRATION</p><h2>${esc(name)}</h2><div class="visual-plan-meta"><span>${esc(p.details?.occasion||'Celebration')}</span><span>${esc(dateLabel(p.details?.date))}</span><span>${esc(p.details?.location||'Location to be confirmed')}</span><span>${p.details?.guests?esc(p.details.guests)+' guests':'Guest list in progress'}</span></div></div><div class="visual-plan-message"><span>✦</span><div><strong>It’s coming together.</strong><p>Your chosen suppliers and the pieces still to arrange, all in one lovely plan.</p></div></div><div class="visual-services">${cards}</div><div class="visual-plan-footer"><button type="button" class="visual-back">← Back to My Planning Portal</button><button type="button" class="visual-print">Print / save plan</button></div></div>`}
function openVisualPlan(){const p=readPlan(),w=document.querySelector('#workspace');if(!p||!w)return;w.dataset.portalBackup=w.innerHTML;w.innerHTML=visualPlan(p,readStatuses());w.scrollIntoView({behavior:'smooth',block:'start'});w.querySelectorAll('.visual-back').forEach(b=>b.addEventListener('click',()=>{w.innerHTML=w.dataset.portalBackup||'';delete w.dataset.portalBackup;requestAnimationFrame(upgradePortal)}));w.querySelector('.visual-print')?.addEventListener('click',()=>window.print());w.querySelectorAll('[data-book-service]').forEach(b=>b.addEventListener('click',()=>{const n=readStatuses();n[b.dataset.bookService]='Booked';writeStatuses(n);w.innerHTML=visualPlan(readPlan(),n);openVisualEvents(w)}));}
function openVisualEvents(w){w.querySelectorAll('.visual-back').forEach(b=>b.addEventListener('click',()=>{w.innerHTML=w.dataset.portalBackup||'';delete w.dataset.portalBackup;requestAnimationFrame(upgradePortal)}));w.querySelector('.visual-print')?.addEventListener('click',()=>window.print());w.querySelectorAll('[data-book-service]').forEach(b=>b.addEventListener('click',()=>{const n=readStatuses();n[b.dataset.bookService]='Booked';writeStatuses(n);w.innerHTML=visualPlan(readPlan(),n);openVisualEvents(w)}));}
function upgradePortal(){const w=document.querySelector('#workspace'),portal=document.querySelector('#portal-content');if(!w||!portal||document.querySelector('#service-plan-overview'))return;const p=readPlan();if(!p||p.step!==3||!Array.isArray(p.services)||!p.services.length)return;const saved=readStatuses();const cards=p.services.filter(id=>categories[id]).map(id=>{const c=categories[id],status=statusOrder.includes(saved[id])?saved[id]:inferredStatus(id,p),count=(p.suppliers||[]).filter(x=>x.category===id).length;return `<article class="service-plan-card ${status==='Booked'?'is-booked':''}"><div class="service-plan-copy"><div class="service-plan-heading"><h3>${esc(c.name)}</h3><span class="service-plan-status">${esc(status)}${status==='Booked'?' ✓':''}</span></div><p>${count?count+' vendor'+(count===1?'':'s')+' saved':'Ready when you are'}</p></div><div class="service-plan-actions"><select data-plan-status="${esc(id)}">${statusOrder.map(x=>`<option ${x===status?'selected':''}>${esc(x)}</option>`).join('')}</select><a class="service-find-button" href="/planner#${c.hash}">${esc(c.find)} →</a></div></article>`}).join('');const finished=p.services.filter(id=>(saved[id]||inferredStatus(id,p))==='Booked').length,o=document.createElement('section');o.id='service-plan-overview';o.className='service-plan-overview';o.innerHTML=`<div class="service-plan-intro"><div><p class="service-plan-kicker">YOUR CELEBRATION PLAN</p><h3>${finished} of ${p.services.length} services sorted</h3><p>Work through each part at your own pace. Your next step is always right here.</p></div><button type="button" class="view-visual-plan">View my celebration plan →</button><div class="service-plan-progress"><span style="width:${p.services.length?finished/p.services.length*100:0}%"></span></div></div><div class="service-plan-list">${cards}</div>`;portal.prepend(o);o.querySelector('.view-visual-plan')?.addEventListener('click',openVisualPlan);o.querySelectorAll('[data-plan-status]').forEach(x=>x.addEventListener('change',()=>{const n=readStatuses();n[x.dataset.planStatus]=x.value;writeStatuses(n);o.remove();upgradePortal()}));}
function addStartNew(){const nav=document.querySelector('.planner-header-actions');if(!nav||document.querySelector('.start-new-celebration'))return;const a=document.createElement('a');a.className='start-new-celebration';a.href='/celebration-planner/index.html?fresh=1';a.textContent='+ Start a new celebration';a.addEventListener('click',e=>{if(!confirm('Start a new celebration? Your current plan will be kept as a backup in this browser.'))e.preventDefault()});nav.insertBefore(a,nav.firstChild)}
function run(){addStartNew();upgradePortal()}const observer=new MutationObserver(()=>requestAnimationFrame(run));window.addEventListener('DOMContentLoaded',()=>{run();const w=document.querySelector('#workspace');if(w)observer.observe(w,{childList:true,subtree:true})});window.addEventListener('storage',run);
})();